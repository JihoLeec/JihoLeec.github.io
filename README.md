# JihoLeec.github.io

Astro로 만든 포트폴리오 사이트입니다.

## 로컬 실행

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:4321` 을 여세요.

## 배포

1. GitHub repo Settings → Pages → Source를 **GitHub Actions**로 설정
2. `main`에 push하면 `.github/workflows/deploy.yml`이 빌드·배포합니다
